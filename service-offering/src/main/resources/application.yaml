server:
  port: 5004

spring:
  application:
    name: service-offering

  datasource:
    url: jdbc:mysql://localhost:3306/service_offering_db?createDatabaseIfNotExist=true
    username: root
    password: sam123
    driver-class-name: com.mysql.cj.jdbc.Driver

  # jpa config
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
