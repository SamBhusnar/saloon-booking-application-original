# server port------------------
server:
  port: 5002
spring:
  application:
    name: saloon-service
  datasource:
    url: jdbc:mysql://localhost:3306/salon_db?createDatabaseIfNotExist=true
    username: root
    password: sam123
    driver-class-name: com.mysql.cj.jdbc.Driver

  # jpa config
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true